import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#4BFFA5",
    },
    secondary: {
      main: "#A4A4A4",
    },
    error: {
      main: "#FFFFFF",
    },
  },
});

export default theme;
